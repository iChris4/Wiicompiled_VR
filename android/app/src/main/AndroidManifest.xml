<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- The launcher's disc image extraction runs as a foreground service that keeps the CPU awake. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- The Khronos loader brokers the system OpenXR runtime through this permission. -->
    <uses-permission android:name="org.khronos.openxr.permission.OPENXR_SYSTEM" />

    <queries>
        <provider android:authorities="org.khronos.openxr.runtime_broker;org.khronos.openxr.system_runtime_broker" />
        <intent>
            <action android:name="org.khronos.openxr.OpenXRRuntimeService" />
        </intent>
        <intent>
            <action android:name="org.khronos.openxr.OpenXRApiLayerService" />
        </intent>
    </queries>

    <uses-feature android:name="android.hardware.vulkan.level" android:required="true" android:version="1" />
    <uses-feature android:name="android.hardware.vulkan.version" android:required="true" android:version="0x00401000" />
    <uses-feature android:name="android.hardware.vr.headtracking" android:required="true" android:version="1" />
    <uses-feature android:name="android.software.xr.api.openxr" android:required="true" />
    <!-- XR_FB_passthrough around the menu screen: without this flag Horizon OS keeps the app
         passthrough-disabled and the passthrough layer composites nothing. No camera permission. -->
    <uses-feature android:name="com.oculus.feature.PASSTHROUGH" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.gamepad" android:required="false" />

    <application
        android:allowBackup="false"
        android:appCategory="game"
        android:hasFragileUserData="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.WiiCompiled.Quest">

        <meta-data android:name="com.oculus.supportedDevices" android:value="${mkwQuestSupportedDevices}" />
        <meta-data android:name="com.oculus.vr.focusaware" android:value="true" />
        <!-- Let the runtime move CPU budget to the GPU; Aurora's replay is GPU-bound. -->
        <meta-data android:name="com.oculus.trade_cpu_for_gpu_amount" android:value="-1" />

        <!-- The setup panel: Play starts QuestActivity on modern Horizon OS; the
             Quest 1 flavour exposes it as a separate Settings library entry. -->
        <activity
            android:name=".launcher.LauncherActivity"
            android:configChanges="keyboard|keyboardHidden|navigation|orientation|screenLayout|screenSize|smallestScreenSize|uiMode|density"
            android:exported="true"
            android:resizeableActivity="true"
            android:theme="@style/Theme.WiiCompiled.Launcher">
            <!-- Horizon OS panel size. -->
            <layout
                android:defaultWidth="1024dp"
                android:defaultHeight="640dp"
                android:minWidth="760dp"
                android:minHeight="520dp" />
        </activity>

        <!-- Extracts the player's disc image and imports games built on a PC (launcher process). -->
        <service
            android:name=".launcher.GameSetupService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- The immersive game, as DolphinXR's EmulationActivity: VR category, no
             launcher entry, still exported so adb can start it directly. Its own
             process, which QuestActivity ends when the game closes: SDL and the
             runtime cannot start twice in one process. -->
        <activity
            android:name=".QuestActivity"
            android:configChanges="keyboard|keyboardHidden|navigation|orientation|screenLayout|screenSize|smallestScreenSize|touchscreen|uiMode|density"
            android:excludeFromRecents="false"
            android:exported="true"
            android:launchMode="singleTask"
            android:preferMinimalPostProcessing="true"
            android:process=":game"
            android:resizeableActivity="false"
            android:screenOrientation="landscape">
            <property
                android:name="android.window.PROPERTY_XR_ACTIVITY_START_MODE"
                android:value="XR_ACTIVITY_START_MODE_FULL_SPACE_UNMANAGED" />
        </activity>
    </application>

</manifest>
