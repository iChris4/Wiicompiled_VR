<?xml version="1.0" encoding="utf-8"?>
<!-- One licence in the selector: the chosen one takes the card's colour (ProfileSelector). -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/neutral_900" android:state_selected="true" />
    <item android:drawable="@android:color/transparent" />
</selector>
