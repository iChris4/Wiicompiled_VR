<?xml version="1.0" encoding="utf-8"?>
<!-- WheelWizard's MiiCarouselWindow (Views/Popups/MiiManagement/MiiCarouselWindow.axaml): the whole
     Mii, turned by dragging it (Mii3DRender). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingTop="4dp"
    android:paddingEnd="24dp">

    <FrameLayout
        android:layout_width="440dp"
        android:layout_height="300dp">

        <ProgressBar
            android:id="@+id/mii_view_loading"
            android:layout_width="96dp"
            android:layout_height="96dp"
            android:layout_gravity="center"
            android:indeterminateTint="@color/neutral_700" />

        <TextView
            android:id="@+id/mii_view_message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:maxWidth="360dp"
            android:textColor="@color/neutral_400"
            android:textSize="14sp"
            android:visibility="gone" />

        <ImageView
            android:id="@+id/mii_view_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@string/room_view_mii"
            android:scaleType="fitCenter" />
    </FrameLayout>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:text="@string/mii_view_hint"
        android:textColor="@color/neutral_500"
        android:textSize="12sp" />
</LinearLayout>
