<?xml version="1.0" encoding="utf-8"?>
<!-- A player in a room, the search or the leaderboard (WheelWizard's PlayerListItem): their Mii,
     name and friend code, VR, badges, View Room on the leaderboard, leaderboard place and whether
     they host the room. -->
<org.wiicompiled.quest.launcher.TapRow xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_browser_item"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="7dp"
    android:paddingEnd="7dp">

    <FrameLayout
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginTop="3dp"
        android:layout_marginBottom="3dp"
        android:background="@drawable/bg_mii_circle"
        android:clipToOutline="true">

        <ImageView
            android:id="@+id/player_placeholder"
            android:layout_width="26dp"
            android:layout_height="26dp"
            android:layout_gravity="center"
            android:importantForAccessibility="no"
            android:src="@drawable/ic_user"
            android:tint="@color/neutral_600" />

        <ImageView
            android:id="@+id/player_mii"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:importantForAccessibility="no" />
    </FrameLayout>

    <LinearLayout
        android:layout_width="170dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/player_name"
            style="@style/Launcher.FieldLabel"
            android:layout_marginBottom="0dp"
            android:ellipsize="end"
            android:singleLine="true" />

        <TextView
            android:id="@+id/player_code"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/neutral_400"
            android:textSize="14sp" />
    </LinearLayout>

    <LinearLayout
        android:layout_width="90dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:orientation="vertical">

        <TextView
            style="@style/Launcher.FieldLabel"
            android:layout_marginBottom="0dp"
            android:text="@string/room_vr"
            android:tooltipText="@string/room_vr_full" />

        <TextView
            android:id="@+id/player_vr"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/neutral_400"
            android:textSize="14sp" />
    </LinearLayout>

    <FrameLayout
        android:layout_width="0dp"
        android:layout_height="40dp"
        android:layout_weight="1">

        <HorizontalScrollView
            android:id="@+id/player_badges_box"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginEnd="6dp"
            android:background="@drawable/bg_badge_box"
            android:padding="4dp"
            android:scrollbars="none">

            <LinearLayout
                android:id="@+id/player_badges"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:gravity="center_vertical"
                android:orientation="horizontal" />
        </HorizontalScrollView>
    </FrameLayout>

    <!-- The leaderboard's View Room, for a player in a room now: a button, which TapRow lets through. -->
    <ImageView
        android:id="@+id/player_join"
        android:layout_width="30dp"
        android:layout_height="24dp"
        android:layout_marginEnd="8dp"
        android:background="@drawable/bg_play_button"
        android:clickable="true"
        android:contentDescription="@string/leaderboard_view_room"
        android:focusable="true"
        android:paddingStart="8dp"
        android:paddingTop="5dp"
        android:paddingEnd="8dp"
        android:paddingBottom="5dp"
        android:src="@drawable/ic_road"
        android:tint="@color/play_button_content"
        android:tooltipText="@string/leaderboard_view_room"
        android:visibility="gone" />

    <TextView
        android:id="@+id/player_top"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="10dp"
        android:background="@drawable/bg_top_label"
        android:fontFamily="sans-serif-medium"
        android:paddingStart="10dp"
        android:paddingTop="6dp"
        android:paddingEnd="10dp"
        android:paddingBottom="6dp"
        android:textColor="@android:color/white"
        android:textSize="13sp" />

    <ImageView
        android:id="@+id/player_host"
        android:layout_width="16dp"
        android:layout_height="16dp"
        android:layout_marginEnd="10dp"
        android:contentDescription="@string/room_open_host"
        android:src="@drawable/ic_lock_open"
        android:tint="@color/primary_400"
        android:tooltipText="@string/room_open_host" />
</org.wiicompiled.quest.launcher.TapRow>
